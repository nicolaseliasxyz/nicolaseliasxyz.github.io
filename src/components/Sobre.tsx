import { VFlow } from "bold-ui";
import { css } from "@emotion/core";

function Sobre() {
  return (
    <VFlow
      style={css`
        align-items: center;
        margin-bottom: 5rem;
      `}
    ></VFlow>
  );
}

export default Sobre;
